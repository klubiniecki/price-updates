{
  "name": "crypto-price-telegram-bot",
  "version": "1.0.0",
  "description": "Daily crypto price Telegram bot scheduler",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "postinstall": "npm run build",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": ["crypto", "telegram", "bot", "scheduler", "typescript"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "node-cron": "^3.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "@types/node-cron": "^3.0.8",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}