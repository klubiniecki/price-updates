{
  "name": "crypto-price-emailer",
  "version": "1.0.0",
  "description": "Daily crypto price email scheduler",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "postinstall": "npm run build",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": ["crypto", "email", "scheduler", "typescript"],
  "author": "krisl",
  "license": "MIT",
  "dependencies": {
    "node-cron": "^3.0.2",
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "@types/node-cron": "^3.0.8",
    "@types/nodemailer": "^6.4.13",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
